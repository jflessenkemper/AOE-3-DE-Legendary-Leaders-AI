name: StringTable Validation

on:
  push:
  pull_request:

jobs:
  stringtable-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Validate StringTables
        run: python tools/validation/validate_stringtables.py
